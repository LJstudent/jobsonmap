version: "3.9"

services:
  postgres:
    image: postgres:17.8
    container_name: jobsonmap-postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: